# This repository no longer uses Git LFS. The payloads below were exported
# back to normal Git objects (git lfs migrate export) because the account's
# LFS budget was exhausted, which broke every clone. They stay marked -text so
# Git never applies end-of-line conversion to them.
data/runs/**/replay/*.json -text
data/submissions/**/replay/*.json -text
data/replays/**/*.json -text
data/logs/**/*.json -text
data/kaggle_top100/replays/**/*.json -text
data/kaggle_top100/logs/**/*.json -text
data/submissions/*.zip -text
data/runs/**/*.zip -text
data/ml/**/processed/*.csv -text
data/ml/**/processed/*.csv.gz -text
data/ml/**/processed/*.parquet -text
data/ml/**/processed/matrix/** -text
data/ml/**/processed.zip -text
data/ml/**/models/*.joblib -text
data/ml/**/models/*.txt -text
data/ml/**/self_play/**/*.jsonl -text
